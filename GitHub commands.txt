GitHub commands:

$ git remote
	See the remote of the repository.
$ git remote -v
	See the remote of the repository with more informations.
$ git remote add [name] [adress]
	Add the repository on GitHub as a remote

$ git push [remote] [branch]
	Send the changes to the remote
